# Local development overrides for `docker-compose.yml`.
# Copy to `.env` and edit if your host machine already runs services on the
# default ports. The compose file itself ships with professional defaults.
#
# Example: keep host Postgres on 5432 and Orbiteus on 5433.
#
# POSTGRES_HOST_PORT=5433
# REDIS_HOST_PORT=6380
